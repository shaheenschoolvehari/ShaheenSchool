const fs = require('fs'); let c = fs.readFileSync('D:/peronal/SMS_Pern/server/routes/students.js', 'utf8'); const match = c.match(/LEFT JOIN app_users u ON s\\\.user_id = u\\.id[\\s\\S]{1,50}const params = \\[\\];/); if (match) { let rep = 'LEFT JOIN app_users u ON s.user_id = u.id\\n              WHERE 1=1\;\\n          const params = [];'; c = c.replace(match[0], rep); fs.writeFileSync('D:/peronal/SMS_Pern/server/routes/students.js', c); console.log('Fixed file'); }
